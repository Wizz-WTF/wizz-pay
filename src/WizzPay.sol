/// SPDX-License-Identifier: GPL-3.0-or-later
pragma solidity >=0.8.0;

contract WizzPay {
    // functions to setup different style of nft payment streams

    // a) NFTX - drop nft in pool and stream the tokens that come out

    // b) Swap ETH - to nftx tokens and start streaming those

    // c) specific NFT - let's store it in this contract and drop a lock on it until the stream has ended


}